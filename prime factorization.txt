void primefactorization(ll n)
{
    for(int i=0;i<primes.size() && primes[i]*primes[i]<=n;i++)
    {
        while(n%primes[i]==0)
        {
            n/=primes[i];
            cout<<primes[i]<<" ";
        }
    }
    if(n>1)
        cout<<n<<" ";
}